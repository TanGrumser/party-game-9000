services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY:-local/party-game-9000}:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
